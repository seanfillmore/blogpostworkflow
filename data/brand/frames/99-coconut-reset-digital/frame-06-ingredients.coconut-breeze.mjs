// Per-scent because the real list differs: Coconut Breeze adds organic coconut oil
// extract, Pure Unscented has no essential oils at all. The count on the frame has
// to be that scent's actual count.
import { ingredientsFrame } from './ingredients-frame.mjs';

export default ingredientsFrame({
  name: 'frame-06-ingredients-coconut-breeze',
  variant: 'Coconut Breeze',
});
