/** Hand Soap Set — the four scents. Identical across configurations; duplicated per Configuration because every media must be scoped. */
import { rangeFrame } from './hss-frames.mjs';
export default rangeFrame("3 pumps + body lotion");
