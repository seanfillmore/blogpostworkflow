// Shopify PDP gallery version: carries the live subscription offer and the refund
// guarantee. Both are verified against live data before the render.
import { ingredientsFrame } from './ingredients-frame.mjs';

export default ingredientsFrame({ name: 'coconut-lotion-ingredients-pdp', offer: true });
