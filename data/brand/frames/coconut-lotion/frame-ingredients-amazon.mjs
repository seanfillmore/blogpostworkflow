// Amazon secondary-image version: no offer badge and no guarantee bar, because
// Amazon does not permit price or promotional terms on listing images. It can
// never be the main image either — the product must be the subject there.
import { ingredientsFrame } from './ingredients-frame.mjs';

export default ingredientsFrame({ name: 'coconut-lotion-ingredients-amazon', offer: false });
